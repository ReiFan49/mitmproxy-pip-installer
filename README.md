# mitmproxy PIP Installer

Launches `pip install` upon booting `mitm`-related instances.

## Options

- `extra_packages`, a set of packages to be installed upon booting or reconfiguring currently running `mitm`-related instances.
